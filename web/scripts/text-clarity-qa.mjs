import {chromium,expect} from '@playwright/test';
const b=await chromium.launch({channel:'msedge',headless:true,args:['--enable-unsafe-swiftshader']});const p=await b.newPage({viewport:{width:1440,height:960},deviceScaleFactor:2});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4175/',{waitUntil:'networkidle'});await expect(p.locator('.world-preloader')).toHaveCount(0);await p.waitForTimeout(2400);expect(await p.locator('.home-panel').evaluate(e=>e.getBoundingClientRect().width)).toBe(250);await p.screenshot({path:'artifacts/sharper-world.png'});expect(errors).toEqual([]);console.log('PASS: compact panel, rendered high-density scene, no page errors');await b.close();
