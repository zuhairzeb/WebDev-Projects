import {chromium,expect} from '@playwright/test';
const b=await chromium.launch({channel:'msedge',headless:true,args:['--enable-unsafe-swiftshader']});const p=await b.newPage({viewport:{width:1366,height:768}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4175/',{waitUntil:'networkidle'});await expect(p.locator('.world-preloader')).toHaveCount(0);await p.waitForTimeout(2200);await p.screenshot({path:'artifacts/solid-background-home.png'});await p.getByRole('navigation',{name:'World destinations'}).getByRole('button',{name:/services/i}).click();await expect(p.locator('.world-app')).toHaveAttribute('data-zone','services',{timeout:8000});await p.waitForTimeout(900);await p.screenshot({path:'artifacts/solid-background-services.png'});await p.getByRole('button',{name:'WORLD MAP ↗'}).click();await expect(p.locator('.world-map-panel')).toBeVisible();expect(errors).toEqual([]);console.log('PASS home, Services arrival and map; no page errors');await b.close();
