import {chromium} from '@playwright/test';import fs from 'node:fs/promises';const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage();await page.goto('http://127.0.0.1:5175/');const image=await page.evaluate(async()=>{const v=document.createElement('video');v.src='/about%20avator.mp4';v.muted=true;await new Promise((resolve,reject)=>{v.onloadeddata=resolve;v.onerror=reject;});v.currentTime=.3;await new Promise(resolve=>v.onseeked=resolve);const c=document.createElement('canvas');c.width=v.videoWidth;c.height=v.videoHeight;c.getContext('2d').drawImage(v,0,0);return c.toDataURL('image/png').split(',')[1];});await fs.writeFile('artifacts/original-avatar-reference.png',Buffer.from(image,'base64'));await browser.close();
